# Synthetic Media Fixture

`media.ts` generates a one-second, 8 kHz, mono PCM WAV tone entirely in memory. The waveform and HTTP protocol server are original test code in this repository; no production or licensed course bytes are used or written to disk.

The loopback-only server exercises complete and partial responses, unsatisfied ranges, omitted range advertisement, octet-stream MIME, expiry, truncation, unsupported bytes, and same-origin/cross-origin redirects. Its ledger records only structural request facts and never header values or query strings.
