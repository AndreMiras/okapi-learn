export default function LearningLoading() {
  return (
    <div
      className="relative overflow-hidden rounded-[clamp(1rem,4vw,2rem)] border border-[#16324f26] bg-[#fffdf7] p-[clamp(1.5rem,5vw,3rem)] shadow-[0_18px_50px_#16324f18]"
      aria-busy="true"
    >
      <p>Opening the learning notebook...</p>
    </div>
  );
}
